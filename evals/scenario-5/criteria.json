{
  "type": "weighted_checklist",
  "context": "Evaluates whether the agent implements a correct Kubernetes admission control policy in Rego using the kube-mgmt pattern: package kubernetes.admission, deny contains msg, and input.request.object for the Pod spec.",
  "checklist": [
    {
      "name": "package kubernetes.admission",
      "description": "The policy uses `package kubernetes.admission` (the canonical package for OPA kube-mgmt admission control)",
      "max_score": 15
    },
    {
      "name": "import rego.v1",
      "description": "The policy file includes `import rego.v1`",
      "max_score": 10
    },
    {
      "name": "deny contains msg pattern",
      "description": "The policy uses `deny contains msg if { ... }` to collect violation messages",
      "max_score": 20
    },
    {
      "name": "Checks input.request.kind.kind",
      "description": "The deny rule checks `input.request.kind.kind == \"Pod\"` to scope the policy to Pods only",
      "max_score": 15
    },
    {
      "name": "Deny rule for privileged containers",
      "description": "The deny rule iterates over `input.request.object.spec.containers` and triggers when `container.securityContext.privileged == true`",
      "max_score": 30
    },
    {
      "name": "Tests pass",
      "description": "All tests pass when running `opa test . -v` (if tests are present)",
      "max_score": 10
    }
  ]
}
