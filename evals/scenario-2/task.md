# Task: Docker Daemon Authorization Policy

Write a Rego policy that prevents containers from being launched in privileged mode or with a disabled seccomp profile. The system uses `opa-docker-authz`.
